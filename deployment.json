{
    "package": {
        "name": "node-wiring-c", // Bintray package name
        "repo": "inaetics", // Bintray repository name
        "subject": "bpetri" // Bintray subject (user or organization)
    },

    "version": {
        "name": "0.5"
    },

    "files":
        [
        {"includePattern": "deploy/(.*)", "uploadPattern": "$1"}
        ],
    "publish": true
}
